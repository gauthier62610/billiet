# CLAUDE.md

Guidance for AI assistants (and humans) working in this repository.

> **Status: empty repository (starter scaffold).**
> As of the last update this repo contained no source code, build files, or
> history — only this document. The sections below are a **template** to be
> filled in once real code lands. Do **not** treat any placeholder here as an
> established fact about the codebase; replace each section with what is
> actually true when you add code, and delete this banner once the project has
> a real structure to document.

## Project overview

- **Repository:** `gauthier62610/billiet`
- **Purpose:** _TODO — describe what this project does in 1–3 sentences._
- **Language / stack:** _TODO — e.g. TypeScript/Node, Python, Go, …_
- **Status:** Pre-initialization. No application code committed yet.

## Repository structure

_TODO — document the top-level layout once it exists, e.g.:_

```
src/        # application source
tests/      # test suite
docs/       # documentation
```

## Getting started

_TODO — fill in when the toolchain is chosen. Typical shape:_

```bash
# install dependencies
# <package-manager> install

# run the app
# <run command>
```

## Development workflows

_TODO — record the real commands as they are added:_

- **Build:** _TODO_
- **Run / dev server:** _TODO_
- **Test:** _TODO_
- **Lint / format:** _TODO_
- **Type-check:** _TODO_

Update this section whenever a new script or tool is introduced so assistants
can run the right command without guessing.

## Conventions

_TODO — capture coding conventions as they emerge (naming, module layout,
error handling, commit style, etc.). Prefer matching existing code over
imposing new patterns._

## Git & branching

- **Default branch:** _TODO — confirm once the first commit is pushed
  (commonly `main`)._
- Create a feature branch for changes; do not commit directly to the default
  branch.
- Write clear, descriptive commit messages.
- Open a pull request for review; only merge after checks pass.
- Only commit or push when explicitly asked.

## Notes for AI assistants

- This file currently describes an **empty** repository. If you are asked to
  "document the codebase" and find no code, say so plainly rather than
  inventing structure — then update this scaffold with real information as it
  becomes available.
- Keep this document in sync with reality: when you add tooling, tests, or
  structure, update the relevant section in the same change.
